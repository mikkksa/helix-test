from distutils.core import setup

setup(
    name='std',
    version='',
    packages=['api', 'api.migrations', 'std', 'rest', 'rest.migrations', 'loginsys', 'loginsys.migrations', 'studtests',
              'studtests.migrations'],
    url='',
    license='',
    author='mike',
    author_email='',
    description='', requires=['django']
)
